# Copy to .env and fill in. Every collector fails loudly with setup
# instructions rather than crashing if its keys are missing.

# --- Reddit ---
# No client id/secret needed — Reddit no longer reliably issues free OAuth
# "script" app credentials for a student/non-commercial account, so
# reddit_collect.py uses Reddit's public unauthenticated .json endpoints
# instead (see problem_statement.md §9). Only a descriptive User-Agent is
# needed; the default below is fine as-is.
REDDIT_USER_AGENT=discovery-engine-myntra-research/0.1 (student grad project)

# --- Groq (free tier: https://console.groq.com/keys) ---
GROQ_API_KEY=

# Optional extra keys for more daily budget. The binding limit is 200,000
# tokens per DAY, which one key exhausts after roughly 200-350 extractions;
# the client rotates to the next key automatically when it sees a 429 with a
# long retry-after (see problem_statement.md §23).
#
# These MUST be from SEPARATE Groq accounts. Keys generated inside one
# account share a single daily pool, so a second key from the same account
# adds nothing. Numbered vars or a comma-separated GROQ_API_KEYS both work.
GROQ_API_KEY_2=
# GROQ_API_KEYS=key_one,key_two,key_three
